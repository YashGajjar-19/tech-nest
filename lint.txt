
D:\tech-nest\src\components\sections\TrendingDevices.jsx
  21:45  error  Error: Cannot call impure function during render

`Date.now` is an impure function. Calling an impure function can produce unstable results that update unpredictably when the component happens to re-render. (https://react.dev/reference/rules/components-and-hooks-must-be-pure#components-and-hooks-must-be-idempotent).

  19 |     const displayProducts = [...(products || [])].sort((a, b) => {
  20 |         if (activeTab === "latest") {
> 21 |             return new Date(b.created_at || Date.now()) - new Date(a.created_at || Date.now());
     |                                             ^^^^^^^^^^ Cannot call impure function
  22 |         }
  23 |         if (activeTab === "top") {
  24 |             return b.slug.localeCompare(a.slug);                                          react-hooks/purity
  21:84  error  Error: Cannot call impure function during render

`Date.now` is an impure function. Calling an impure function can produce unstable results that update unpredictably when the component happens to re-render. (https://react.dev/reference/rules/components-and-hooks-must-be-pure#components-and-hooks-must-be-idempotent).

  19 |     const displayProducts = [...(products || [])].sort((a, b) => {
  20 |         if (activeTab === "latest") {
> 21 |             return new Date(b.created_at || Date.now()) - new Date(a.created_at || Date.now());
     |                                                                                    ^^^^^^^^^^ Cannot call impure function
  22 |         }
  23 |         if (activeTab === "top") {
  24 |             return b.slug.localeCompare(a.slug);   react-hooks/purity

Γ£û 2 problems (2 errors, 0 warnings)

